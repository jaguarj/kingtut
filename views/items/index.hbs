<!-- <h1>TUTS List</h1>
	<ul>
		{{#each items}}
			<li>{{this.name}}</li>
				<div>
					<form action="/users/{{../user.id}}/items/{{this.id}}?_method=DELETE" method="POST">
						<input type="submit" value="Delete Item"/>
					</form>
				</div>
				<br />
				<! <a href="/users/{{this.id}}">{{this.tuts}}</a> -->
	<!-- 	{{/each}}
		<div>
			<a href="/users/{{user.id}}/items/new">Add New Item</a>
		</div>
	</ul>


 -->

